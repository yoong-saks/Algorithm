#include <iostream>

using namespace std;

int main() {
    cin.tie(NULL);
    ios_base::sync_with_stdio(false);
    
    long long a;

    cin >> a;

    cout << (a * (a - 1)) / 2 << '\n';
    
    cout << 2 << '\n';
}